# Packaging

Package the extension with: `vsce package --yarn`

The --yarn flag is required to get the .vsix built with a local package in the package.json [ref](https://github.com/microsoft/vscode-vsce/issues/381).
